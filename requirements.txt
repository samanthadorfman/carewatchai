ultralytics>=8.3.0
lap==0.5.13   # ByteTrack dep; ultralytics lazily auto-installs this mid-run
              # if missing, which crashed CI ("corrupted double-linked list")
              # since the new native extension isn't safe to use without a
              # process restart. Pinning here installs it upfront instead.
opencv-python-headless>=4.9.0
numpy>=1.26.0
pandas>=2.2.0
streamlit>=1.35.0
Pillow>=10.3.0
sqlite-utils>=3.36
python-dotenv>=1.0.0
kaggle
kagglesdk==0.1.31   # 0.1.32 is broken: ModuleNotFoundError on kagglesdk.competitions.legacy
scikit-learn>=1.4.0
joblib>=1.3.0
